@model CombatLink.Application.ViewModels.LogInViewModel
@{
    ViewData["Title"] = "Log In";
}

<div class="container">
    <div class="row login-box shadow-lg rounded overflow-hidden">
        <!-- Mobile image banner (on top) -->
        <div class="d-block d-md-none login-image-banner">
            <img src="~/images/LoginIllustration.png" alt="Login Illustration" />
        </div>

        <!-- Login form -->
        <div class="col-12 col-md-6 d-flex align-items-center login-wrapper secondary-foreground">
            <div class="w-100 login-form-container">
                <h2 class="mb-4 text-center text-md-start">Log In</h2>

                <form asp-action="LogIn" method="post">
                    <div asp-validation-summary="All" class="text-danger mb-3"></div>

                    <div class="mb-3">
                        <label asp-for="Email" class="form-label"></label>
                        <input asp-for="Email" class="form-control" required />
                        <span asp-validation-for="Email" class="text-danger"></span>
                    </div>

                    <div class="mb-3">
                        <label asp-for="Password" class="form-label"></label>
                        <input asp-for="Password" type="password" class="form-control" required />
                        <span asp-validation-for="Password" class="text-danger"></span>
                    </div>

                    <div class="mb-3 text-center text-md-start">
                        <a asp-controller="User" asp-action="Register">Don't have an account? Register!</a>
                    </div>

                    <button type="submit" class="mainButton w-100">Log In</button>
                </form>
            </div>
        </div>

        <!-- Desktop image side -->
        <div class="col-md-6 d-none d-md-block p-0">
            <div class="login-image-container">
                <img src="~/images/LoginIllustration.png" alt="Login Illustration" />
            </div>
        </div>
    </div>
</div>
